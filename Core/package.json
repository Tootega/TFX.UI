{
  "name": "tfx.ui.core",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build && npm run build-css",
    "build-css": "sass src/styles:public/styles --style compressed",
    "clean": "tsc --build --clean && rm -rf public/js public/styles",
    "watch": "concurrently \"tsc --watch\" \"npm run watch-css\"",
    "watch-css": "sass --watch src/styles:public/styles --style compressed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.13.13",
    "eslint": "^9.23.0",
    "sass": "^1.86.0",
    "typescript": "^5.8.2"
  }
}
